#include "add.h"

/**
 * #include "add.h" сюди моєна не додавати, однак це рекомендується, для перевірка Узгодженості
 * Бо таким чином сама функція і її оголошення з'єднані, якщо наприклад я зміню параметри чи
 * тип який повертається у реалізації і забуду у add.h то отримую помилку, і таким чином, я ніколи
 * не забуду змінити add.h від якої будуть залежати всі файли що імпортують цю функцію
 *
 */

int add(int x, int y){
    return x + y;
}